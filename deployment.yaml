apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-github-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-github-app
  template:
    metadata:
      labels:
        app: my-github-app
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        ports:
        - containerPort: 80
